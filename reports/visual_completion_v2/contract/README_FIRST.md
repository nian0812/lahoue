# LaHoue Visual Completion Kit V2

This kit is the current source of truth for the remaining visual work after the 2026-09-13 runtime pass.

IMPORTANT: this is an authoring/integration contract, not a claim that missing production artwork already exists. The runtime is 25/25 PASS, but full visual sign-off is still blocked by missing modular Restaurant/Table/Depot art, truck directional art, and supplemental animation artwork.

Use `docs/final_missing_assets.md` for exact filenames and route/orientation constraints, and `docs/animation_supplemental_assets.md` for exact frame counts, clip names, directions, FPS, loops, anchors and helicopter parts.

Astra must never call the project visually COMPLETE while any required placeholder/fallback is still visible.
